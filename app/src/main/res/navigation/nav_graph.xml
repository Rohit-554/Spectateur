<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="in.jadu.spectateur.viewImage.ui.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_imageGallery"
            app:destination="@id/imageGallery" />
    </fragment>
    <fragment
        android:id="@+id/imageGallery"
        android:name="in.jadu.spectateur.viewImage.ui.ImageGallery"
        android:label="Image Gallery"
        tools:layout="@layout/fragment_image_gallery" />
</navigation>